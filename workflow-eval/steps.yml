evaluating:
  process:
    process_type: interpolated-script-cmd
    script: |
      mkdir /home/data
      mkdir /home/data/samples
      mkdir /home/models
      tar -xvzf {evalfolder} -C {eval_folder_path}
      ls -lR > {outputfile}
  publisher:
    publisher_type: interpolated-pub
    publish:
      outputfile: '{outputfile}'
  environment:
    environment_type: docker-encapsulated
    image: irinahub/docker-madminer-ml
    imagetag: 'latest'


#python code/evaluation.py {input_evaluation} {eval_folder_path}
#evaluating:
#  process:
#    process_type: interpolated-script-cmd
#    script: |
#      mkdir /home/data
#      mkdir /home/data/samples
#      mkdir /home/models
#      tar -xvzf {evalfolder} -C {eval_folder_path}/training_*
#      python code/evaluation.py {input_evaluation} {eval_folder_path}
#      cp -R  {evalworkdir}
#  publisher:
#    publisher_type: interpolated-pub
#    publish:
#      outputfile: '{outputfile}'
#  environment:
#    environment_type: docker-encapsulated
#    image: irinahub/docker-madminer-ml
#    imagetag: 'latest'
#
#
