evaluating:
  process:
    process_type: interpolated-script-cmd
    script: |
      mkdir /home/data
      mkdir /home/data/samples
      mkdir /home/models
      tar -zxvf {eval_folder} -C {eval_folder_path}
      python code/evaluation.py {input_evaluation} {eval_folder_path}/home/models
      ls -lR > {outputfile}
  publisher:
    publisher_type: interpolated-pub
    publish:
      outputfile: '{outputfile}'
  environment:
    environment_type: docker-encapsulated
    image: irinahub/docker-madminer-ml
    imagetag: 'latest'



#evaluating:
#  process:
#    process_type: interpolated-script-cmd
#    script: |
#      mkdir /home/data
#      mkdir /home/data/samples
#      mkdir /home/models
#      ls -lR > {outputfile}
#  publisher:
#    publisher_type: interpolated-pub
#    publish:
#      outputfile: '{outputfile}'
#  environment:
#    environment_type: docker-encapsulated
#    image: irinahub/docker-madminer-ml
#    imagetag: 'latest'